import XCTest
@testable import UnresponsiveCaptureKit

final class UnresponsiveCaptureKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(UnresponsiveCaptureKit().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
